version: 2.1
jobs:
  build:
    docker:
      - image: microsoft/dotnet:3.1-sdk
    steps:
      - checkout
      - run: dotnet build $SolutionName
workflows:
  version: 2.1
  buildAll:
    jobs:
      - build